#!/bin/sh
# postrm script for zulu-kernel-image
#
# see: dh_installdeb(1)

set -e

export INITRD=Yes
if [ -d "/etc/kernel/postrm.d" ]; then
    run-parts -v --report --exit-on-error --arg=#KERNELRELEASE# --arg=/boot/vmlinux-#KERNELRELEASE# /etc/kernel/postrm.d
fi


# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
